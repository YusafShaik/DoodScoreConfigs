
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/doodscoreuserdb
    username: postgres
    password: user
  jpa:
   hibernate:
     ddl-auto: update
   properties:
     hibernate:
       dialect: org.hibernate.dialect.PostgreSQLDialect

